## How to Run

1. Open the `backpack_tracker` folder.
2. Run `main.py`.
3. Use the menu to test the application.

The `backpack_tracker_buggy` folder contains the intentionally faulty version for the debugging assignment.
